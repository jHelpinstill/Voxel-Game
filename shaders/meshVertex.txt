#version 330 core

layout (location=0) in vec3 vertex_pos;
layout (location=1) in vec3 vertex_color;

out vec3 fragmentColor;

uniform mat4 camera_mat;

void main()
{
	gl_Position = camera_mat * vec4(vertex_pos, 1.0);
	fragmentColor = vertexColor;
}